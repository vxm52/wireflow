ANTHROPIC_API_KEY=

# Abuse guards for the public demo (Upstash Redis, REST API).
# Missing or unreachable: the guards fail open and requests are allowed.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: max generations per day across all users (default 100).
DAILY_MAX=
